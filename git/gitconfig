[color]
	ui = true
[core]
	editor = emacs -nw
	excludesfile = ~/.gitignore
[credential]
	helper = store
[push]
	default = current
[user]
	name = Jeff Coffler
	email = jeff@taltos.com
[help]
	autoCorrect = -1
[log]
	abbrevCommit = true
	decorate = short
[apply]
	ignoreWhitespace = change
[am]
	threeWay = true
[alias]
	nuke = !git clean -fdx && git submodule foreach git clean -fdx 
	smaster = !git submodule foreach git checkout master
	sup = !git submodule foreach git pull
	sync-branch = remote prune origin
